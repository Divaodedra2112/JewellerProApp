import { scale } from './Responsive';

/**
 * Horizontal padding/margin for main screen content (banner, categories, updates, action cards, promotional banner).
 * Use this for consistent horizontal spacing across the Home screen sections.
 */
export const SCREEN_PADDING_HORIZONTAL = scale(12);
